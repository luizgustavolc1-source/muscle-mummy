import { BarChart3, Bot, CalendarDays, LogOut, Users } from "lucide-react";
import { NavLink, Outlet, useNavigate } from "react-router-dom";
import { supabase } from "../../lib/supabase";
import { useSession } from "../../services/session";
export default function AppShell(){const navigate=useNavigate();const {session}=useSession();const email=session?.user?.email??"Coach";async function signOut(){await supabase.auth.signOut();navigate("/login")}return <div className="app-shell"><aside className="sidebar"><div className="brand"><span className="brand-mark">MM</span>Muscle Mummy</div><NavLink className="nav-link" to="/dashboard"><BarChart3 size={19}/><span>Dashboard</span></NavLink><NavLink className="nav-link" to="/clients"><Users size={19}/><span>Clients</span></NavLink><NavLink className="nav-link" to="/calendar"><CalendarDays size={19}/><span>Calendar</span></NavLink><NavLink className="nav-link" to="/ai-secretary"><Bot size={19}/><span>AI Secretary</span></NavLink></aside><main className="shell-main"><header className="topbar"><div><strong>{email.split("@")[0]}</strong><p>Coach workspace</p></div><button className="signout" onClick={signOut}><LogOut size={15}/> Sign out</button></header><Outlet/></main></div>}
